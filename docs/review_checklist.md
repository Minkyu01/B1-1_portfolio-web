# 검증 체크리스트

## 자동 검증

`npm run check`(= `bash tests/run_all.sh`)로 한 번에 실행한다.

- [x] 두 JavaScript 파일과 테스트 파일의 문법(`node --check`)
- [x] 순수 상태·검증·안전한 카드·오류 문구(`test_state.mjs`)
- [x] 가짜 DOM에서 자동 로드, 로딩·성공·빈 결과·403·재시도·형식 오류·요청 취소·기준값 경계·테마·메뉴·문의 폼(`test_main_dom.mjs`)
- [x] HTML 랜드마크, label-for/id, 이미지 alt, 외부 CSS/JS와 `defer`, 인라인 `style`/`onclick` 금지, 금지 라이브러리, `var` 금지(`test_static_contract.py`)
- [x] README의 필수 항목(소개·사용 기술·배포 URL·스크린샷)과 기준값 명시(`test_static_contract.py`)
- [x] 실제 Chrome에서 명세 조항별 종단 검사(`e2e_browser.mjs`)

## 브라우저에서 자동으로 확인하는 명세 항목

`node tests/e2e_browser.mjs`가 조항 번호와 함께 출력한다.

- [x] 320 / 375 / 768 / 1024 / 1440px에서 가로 넘침이 없고 열 수가 바뀜 `[2]` `[4.3]`
- [x] 모바일: 햄버거 클릭 → 메뉴 표시(`classList.toggle('active')`), 다시 클릭 → 숨김 `[4.5-1]`
- [x] 메뉴 클릭 시 중간 위치를 거쳐 부드럽게 이동 `[4.5-2]`
- [x] 맨 위로 버튼: 기준값 미만에서는 숨김·Tab 순서에서 제외, 이상에서 표시, 클릭 시 맨 위로 `[4.5-3]`
- [x] 헤더 배경이 기준값 이상에서 바뀜 `[4.5-4]`
- [x] 다크 모드 전환, `localStorage` 저장, 새로고침 후 유지 `[4.5-5]`
- [x] 스크롤 애니메이션: 화면 밖 섹션은 숨겨졌다가 보이면 나타남 `[4.5-6]`
- [x] 빈 제출·형식 오류·정상 입력, 오류는 입력 칸 근처, 페이지 이동 없음 `[4.6]`
- [x] 페이지를 열면 기본 계정을 자동 요청, 로딩·성공·빈·에러(+재시도)·403·네트워크 실패·404 `[4.8]`
- [x] 사용자명 폼: 잘못된 값은 요청 없이 오류, 올바른 값은 목록 교체 `[4.9]`
- [x] 인라인 `style`·`on*` 속성 없음, 콘솔 에러 없음 `[7]`

## 직접 확인할 항목

- [ ] 키보드만으로 skip link → 메뉴 → 폼 → 맨 위로 버튼까지 Tab 순서가 자연스러움
- [ ] 실제 스마트폰(터치)에서 메뉴와 폼 조작
- [ ] Live Server(VS Code)에서 저장 시 자동 새로고침 — 확장 프로그램이라 자동 검증할 수 없다

## 제출 전 확인

- [x] `docs/screenshots/`에 데스크톱·모바일·다크 모드 스크린샷 (`--live --screenshots`)
- [x] GitHub 저장소에 코드 업로드(`main`, 공개 저장소) — [제출 안내](submission.md)에 기록
- [x] GitHub Pages 켜기 — `gh-pages` 브랜치를 push하자 GitHub가 자동으로 켬 (2026-09-19)
- [x] 배포된 주소에서 `node tests/e2e_browser.mjs --url=<배포 주소>` 통과 — 59/59, `--live` 2/2 (2026-09-19)
- [x] 평가 종료 후 GitHub Pages를 내림 — `gh-pages` 브랜치 삭제 (2026-09-21)
